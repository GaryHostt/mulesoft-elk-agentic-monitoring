<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
    xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
    xmlns:http="http://www.mulesoft.org/schema/mule/http"
    xmlns:mcp="http://www.mulesoft.org/schema/mule/mcp"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/mcp http://www.mulesoft.org/schema/mule/mcp/current/mule-mcp.xsd">

    <configuration-properties file="classpath:config.properties" />

    <http:listener-config name="HTTP_Listener_Config" doc:name="HTTP Listener config">
        <http:listener-connection host="${http.host}" port="${http.port}" />
    </http:listener-config>

    <mcp:config name="MCP_Server_Config" serverName="elk-mcp" serverVersion="1.0.0" doc:name="MCP Server config">
        <mcp:streamable-http-server-connection listenerConfig="HTTP_Listener_Config" mcpEndpointPath="${mcp.endpointPath}" />
    </mcp:config>

    <http:request-config name="Elasticsearch_Request_Config" doc:name="Elasticsearch HTTP Request config">
        <http:request-connection host="${elasticsearch.host}" port="${elasticsearch.port}" protocol="${elasticsearch.protocol}" />
    </http:request-config>

    <!-- Phase 2: Jira, PagerDuty Events API, Slack webhook -->
    <http:request-config name="Jira_Request_Config" doc:name="Jira HTTP Request config">
        <http:request-connection host="${jira.host}" port="443" protocol="HTTPS" />
    </http:request-config>
    <http:request-config name="PagerDuty_Request_Config" doc:name="PagerDuty HTTP Request config">
        <http:request-connection host="events.pagerduty.com" port="443" protocol="HTTPS" />
    </http:request-config>
    <http:request-config name="Slack_Webhook_Request_Config" doc:name="Slack Webhook HTTP Request config">
        <http:request-connection host="hooks.slack.com" port="443" protocol="HTTPS" />
    </http:request-config>

</mule>
